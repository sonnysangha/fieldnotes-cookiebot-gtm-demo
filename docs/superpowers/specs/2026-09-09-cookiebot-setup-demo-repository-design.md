# Cookiebot Setup Demo repository design

## Goal

Publish the existing project under a clearer public GitHub repository name and make the README immediately explain its relationship to the accompanying YouTube video.

## Repository changes

- Rename `sonnysangha/fieldnotes-cookiebot-gtm-demo` to `sonnysangha/Cookiebot-Setup-Demo`.
- Keep the repository public and preserve its existing history.
- Update the local `origin` URL to the renamed repository's canonical HTTPS URL.
- Do not create or delete a second repository.

## README changes

- Change the main heading to `Cookiebot Setup Demo`.
- Open with a concise statement that the repository accompanies a YouTube video demonstrating how to set up Cookiebot and explore its consent-management benefits with Google Tag Manager.
- Keep the existing Cookiebot affiliate signup link prominent and unchanged.
- Retain the affiliate disclosure.
- Update the clone URL and cloned-directory command to use the renamed repository.
- Leave the rest of the tutorial unchanged.

No YouTube URL will be added because one was not provided.

## Verification

- Confirm the README contains the video context and signup link.
- Confirm the renamed GitHub repository is public and reachable at its new URL.
- Confirm the local `origin` points to the new canonical URL.
- Run the repository's relevant documentation and build checks before publishing the final README commit.
